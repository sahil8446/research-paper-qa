# Copy to .env (never commit .env)
ANTHROPIC_API_KEY=

# Optional overrides -- see src/rag/config.py for defaults
# QDRANT_URL=http://localhost:6333   # unset = local on-disk collection, no server needed
# ANSWER_MODEL=claude-opus-5
